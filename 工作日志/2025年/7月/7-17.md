+ [x]  阅读一下ai写的文档
+ [x] 重新编译一下项目看效果（吧agent加进去）
+ [x] 注意流水线问题


+ [x] 整理一下极光项目中关于MVVM的代码，包括七层结构、往下data，viewModel等等的
+ [ ] 拓展复杂组件的dump




TVBaseViewModel中的刷新逻辑：

从 public final boolean updateUI(T data) 开始
preData()
onRecordUpdateStart();
onUpdateUI(data);   --- 这里会调用继承类的方法，一直找到最下面的onUpdateUI
updateUiAsyncInner();



HeadPosterPlayerViewModel、AbsHeadPosterPlayerViewModel、HeadPosterPlayerViewInfo、HiveViewModel、ItemInfoViewModel、TVBaseViewModel
、TVCssViewModel


GridFilmListRankAndReserveViewModel 有问题 这个直接开始看问题，算是完善复杂组件Dump了


今天主要是输出文档