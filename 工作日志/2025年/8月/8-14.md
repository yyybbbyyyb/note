完成了plugin_base，提速把代码CR交上去


+ 完成文档的沉淀（实现细节 + v2.0）
+ 看一下网络库的bug

+ 改单测


------

### 阅读网路库中一些别扭的点：

+ HttpStackHolder的设立
+ parseNetworkResponse和onSuccess处理返回数据两次
+ org.apache.http.legacy的引入
+ get和getResponseImpl为啥responseHandler会有主线程调用的区别
+ String requetUrl = requestHandler.makeRequestUrl(); requestHandler.setUrl(requetUrl);不同层Request，那为啥第一层不直接设置
+ String requetUrl小拼写错误
+ getRespErrorData为什么不沿用到缓存读取失败的里面？
+ 日志中统计网络请求间隔最大值为300秒，先写死。找机会对网络库配置统一优化
+ setHttpStackType这个方法，在Basic中没有调用，是basic不用吗？
+ HttpResponse这个类来自于legacy啊
+ //中转出去，提供改包能力，stream方式暂时屏蔽改包
+ 后续可组织成回调的形式
+ OkHttpStack中builder建了又建的问题？
+ TVNetworkService这个文件的意义就是透传嘛？那为啥不让TVNetwork直接继承ITVNetworkService，然后还是TvGlobalManager集成的INetwork，这里面的设计很乱
+ 工厂模式GlobalManagerFactory这算真工厂吗