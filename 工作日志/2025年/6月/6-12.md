### 继续开发Demo
+ [x] 解决tab和ranklist切换的问题
+ [x] 解决tab和viewPage$RecyclerView的问题


不足的一点：界面切换快了之后还是容易得到焦点


### 代码中的知识点

+ 代码中的知识点

    + 全局的焦点调试`addOnGlobalFocusChangeListener`
    + 反射处理ViewPager2抢占焦点问题
    + viewPager2中的`registerOnPageChangeCallback`
    + 视图的`setOnKeyListener`搭配`event.getAction() == KeyEvent.ACTION_DOWN && keyCode == ...`

    + 千万注意更新UI用acivity的runOnUiThread，不然会闪退

+ 整理注释


### 本周周报+日报