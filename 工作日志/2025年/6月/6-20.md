### 学到的东西

语法相关：

通过写代码熟悉了：
+ IO相关：BufferedReader reader = new BufferedReader(new InputStreamReader(in))
+ WeakReference
+ getSupportFragmentManager()、activity.findViewById(R.id.view_pager);fragmentManager.getFragments()一系列的去定位



发布相关：


需求：

    1. stetho代码提到工蜂
    2. 改下发布相关的脚本，然后发布到内部仓库

    可能得给stetho建个新module，stetho-no-op，所有用到的的对外类都做个空实现，releaseImplementation依赖用
    debugImplementation依赖真的stetho

    理想的话应该是个插件

阅读release.gradle:
    基本能看懂了，也知道了一些配置，比如maven仓库URL，版本，GROUP，task，POM，
