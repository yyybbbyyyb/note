### 工蜂操作文档学习

+ git常用操作（其实很多自己已经很熟悉了）
```git
# 没用过的操作
git cherry-pick <commit>       # 拣选指定 commit
git revert <commit>            # 在当前基础上涂改掉某个历史版本
git diff                       # 查看变更内容
git blame <file>               # 逐行查看文件最新行的变更历史
git reflog                     # 查看本地最近的几次版本变更操作
git remote –v                  # 查看当前远程仓库地址

# tag ref操作用的少
git tag                                          # 查看本地的 tag
git tag <tag_name>  <commit_id>                  # 创建轻量型 tag
git tag <tag_name>  <commit_id> -n "备注信息"      # 创建附注型 tag
git show-ref                                     # 显示本地所有的 ref
git update-ref -d  <ref_name>                    # **高危** 删除指定 ref

# 问题排查与仓库维护相关
git fsck --full                                        # 仓库完整性检查
ssh -T -v git@xxxxx.git                                # 测试 ssh 连接，并打印交互信息（-v）
GIT_CURL_VERBOSE=1 git ls-remote https://xxxxxxx.git   # 查看远端分支，并打印 http 日志（GIT_CURL_VERBOSE=1）
cat ~/.gitconfig                                       # 查看本地配置
cat .git/config                                        # 查看当前仓库配置
git gc                                                 # **高危** 回收未被 ref 引用到的对象
git remote prune origin                                # 对比远端分支，删除掉本地旧的、有冲突的分支

# 分支合并的三种方式
# 普通合并
git checkout target_branch
git merge --no-ff source_branch
# 压缩合并
git checkout target_branch
git merge --squash source_branch
git commit
# 变基合并
git checkout source_branch --detach
git rebase target_branch
git branch target_branch HEAD -f
git checkout target_branch
```

+ 合并请求（和github差不多，这里叫MR）

+ 产品文档：待办、搜索等等，现用现看吧，比如合并请求管理，代码评审之类的




### 配置问题
+ 原来终端zsh的样式是可以导出文件的（这也算是常识了吧），总之这种重复挫轮子的事情就不该有，终于把命令行配置舒服了
+ 同理了，vscode配置也一键挪过来了（天呐，居然还真的想一个一个手动同步，太蠢了）
+ 时间就直接手动暴力吧，真是改不明白定位了

### 开发TVDemoApp
+ git开发操作流程
+ 代码风格检测导入
+ 第三方环境导入
+ 具体开发
+ 最后又学了下工蜂的MR以及review


##### 忘记的或者新学到的东西tips：

+ framelayout、glide、布局居左居右、Gson（内部类带来了问题）
+ Legacy Layout Inspector、RecyclerView（走Adapter那一套）
+ DiffUtil 优化、用stateList处理焦态处理动画等、


##### 自己要解决的问题：

+ dp和px （ok了，这个主要是没理解发的数据是啥意思，现在很通透了）
+ 标题背景（可以不细化UI）
+ 变量提取（这个慢慢做）


##### 问欢哥的问题：

+ 文件夹中的命名（不能子文件夹）（就是这样没办法）
+ banner图片在哪呢？（我们这个没有）
+ 代码检测是不是没生效（自动格式化）



