
+ [ ] 半屏代码合入
+ [x] 核销代码合入
+ [x] rich化入口代码合入
+ [ ] CR一下频控代码

+ [x] 上报act_id，半屏报，并且下发到按钮里
	+ [x] 上报半屏act_id没问题，按钮希望看看能不能后台来下发，act_id半屏请求会带过去，后台组装到按钮的上报字段里
	+ [x] 客户端去同步数据的话可以用mDataAdapter.setLocalReportData(reportData);
	+ 注意一下oneRefreshItems并没有mergeDtReportInfo

+ [x] 再去同步一下迭代文档
+ [x] 记得用720p和4k的模拟器去测试一下兼容性

+ [ ] 等后台发测试环境就merge所有分支去测试
	+ [x] 已经测试详情页入口以及rich化
	+ [ ] 测试半屏回包UI

+ [ ] 阅读20.1的需求
	+ [x] 产出基本的改动代码范围
	+ [ ] 阅读完整原先代码
	+ [ ] 先做代码的优化改版（静态）
	+ [ ] 增加动效


----
代码中的一些学习：
1. @BindNode修饰的canvas一般不会为null，不用判空
2. GlideUtils.into(this, url, canvas, drawableSetter);和RequestBuilder = Glide.with(...).load(url)
GlideUtils.into(this, RequestBuilder, canvas, setter)基本功能等价，但是RequestBuilder 版本可以额外配置，需要尺寸、优先级、占位图、错误图等配置使用 RequestBuilder 版本；