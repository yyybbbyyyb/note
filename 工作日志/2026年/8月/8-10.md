
+ [ ] UI代码合入
	+ [x]  修改半屏UI代码的CR
+ [ ] 逻辑代码合入
+ [ ] CR一下频控代码
+ [x] 催后台要测试环境联调
+ [x] 完成入口协议字段和rich能力的开发
+ [x] 完成上报代码的跟读


+ [x] 上报act_id，半屏报，并且下发到按钮里
	+ [ ] 上报半屏act_id没问题，按钮希望看看能不能后台来下发，act_id半屏请求会带过去，后台组装到按钮的上报字段里
	+ 客户端去同步数据的话可以用mDataAdapter.setLocalReportData(reportData);
	+ 注意一下oneRefreshItems并没有mergeDtReportInfo
+ [x] 合并三个分支到一个temp里做测试

+ [ ] 记得用720p和4k的模拟器去测试一下兼容性

+ [ ] 阅读20.1的需求